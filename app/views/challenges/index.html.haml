%h1 Challenges

- @challenges.each do |challenge|
  %section
    .id= challenge.challenge_id
    %h2.title
      = link_to challenge.title, challenge
    .text= challenge.description
    %time
      = "Start date: #{l(challenge.start_date)}"
    %time
      = "End date: #{l(challenge.end_date)}"
    .meta= "Followers: #{challenge.followers}"

