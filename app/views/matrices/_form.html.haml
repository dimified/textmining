= form_for @matrix do |f|
  - if @matrix.errors.any?
    #error_explanation
      %h2= "#{pluralize(@matrix.errors.count, "error")} prohibited this matrix from being saved:"
      %ul
        - @matrix.errors.full_messages.each do |msg|
          %li= msg

  .actions
    = f.submit 'Save'
